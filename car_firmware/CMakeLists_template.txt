# ${templateWarning}
set(CMAKE_SYSTEM_NAME Generic)
set(CMAKE_SYSTEM_VERSION 1)
${cmakeRequiredVersion}

set(CMAKE_C_COMPILER arm-none-eabi-gcc)
set(CMAKE_CXX_COMPILER arm-none-eabi-g++)
set(CMAKE_ASM_COMPILER arm-none-eabi-gcc)
set(CMAKE_TRY_COMPILE_TARGET_TYPE STATIC_LIBRARY)

project(${projectName} VERSION 1.0.0 LANGUAGES C CXX ASM)

# CubeMX owns these three lists. Project structure and target policy live under cmake/.
set(WL1_CUBEMX_INCLUDE_DIRS ${includes})
set(WL1_CUBEMX_DEFINITIONS ${defines})
file(GLOB_RECURSE WL1_CUBEMX_SOURCES CONFIGURE_DEPENDS ${sources})

include(cmake/firmware.cmake)
